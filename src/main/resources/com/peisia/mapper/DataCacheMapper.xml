<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.peisia.mapper.DataCacheMapper">
	<select id="getRateGrade" resultType="com.peisia.dto.rate.GradeDto">
	    SELECT grade, chance FROM card_grade_table ORDER BY chance
	</select>
	<select id="getDataJob" resultType="com.peisia.dto.game.DataJobDto">
	    SELECT * FROM card_job_table
	</select>
</mapper>
